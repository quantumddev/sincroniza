"""
Paquete core — utilidades puras del backend de Sincroniza.

Módulos disponibles:
    normalizacion  — normalización de rutas Windows
    validaciones   — funciones puras de validación de rutas
    hashing        — hash SHA-256 de archivos por bloques
    fingerprint    — fingerprint determinista de un plan
    glob_matcher   — evaluación de patrones glob sobre rutas relativas
    fechas         — timestamps ISO 8601 y mtime del filesystem
"""
